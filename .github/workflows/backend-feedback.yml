name: Feedback Service CI

on:
  push:
    paths:
      - "backend/feedback-service/**"
      - ".github/workflows/backend-feedback.yml"
      - ".github/workflows/backend-service-ci.yml"
  pull_request:
    paths:
      - "backend/feedback-service/**"
      - ".github/workflows/backend-feedback.yml"
      - ".github/workflows/backend-service-ci.yml"
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/backend-service-ci.yml
    with:
      service_name: Feedback Service
      service_path: backend/feedback-service
      runtime: java
      dockerfile_path: backend/feedback-service/Dockerfile
      java_version: "21"
      compose_service: feedback-service
      deploy_on_main: true
    secrets:
      ec2_host: ${{ secrets.EC2_HOST }}
      ec2_user: ${{ secrets.EC2_USER }}
      ec2_ssh_key: ${{ secrets.EC2_SSH_KEY }}
